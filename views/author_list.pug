extends layout

block content
  h1= title

  ul
    each author in author_list
      li
        a(href=author.url) #{author.name}
        |  (#{author.check_date_of_birth} - #{author.check_date_of_death})

    else
      li There are no authors.